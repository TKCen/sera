# Node.js
node_modules/
dist/
build/
.next/
out/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker
# .dockerignore # we need the docker ignore for correct builds
docker-compose.override.yaml

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project Specific
references/
tmp/
brain/
/workspaces
/.worktrees
/.dogfeed-worktrees

# Go (TUI)
tui/*.exe
tui/*.exe~
tui/*.dll
tui/*.so
tui/*.dylib
tui/*.test
tui/*.out
tui/debug.log

# Standalone lockfiles (workspace root bun.lock is canonical)
web/bun.lock
core/agent-runtime/bun.lock

# Agents
.claude/
.omc/

# Server/debug logs
backend*.log

# Test Results
playwright-report/
test-results/

# Local tooling indexes
.gitnexus/

# core configs
core/config/

# proxy acls
egress-proxy/acls/

# agents memories (root-level runtime data, not source code)
/memory/

# mcp
.mcp.json

# MkDocs build output
site/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.omx/
