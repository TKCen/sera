//! SERA TUI — terminal user interface built with ratatui + crossterm.
//!
//! Replaces the Go TUI (tui/).
//! Migrated last, after the Rust API is stable.

fn main() {
    println!("sera-tui-rs: not yet implemented");
}
