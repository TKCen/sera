{
  "name": "sera-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:dev": "E2E_STACK_MODE=dev playwright test",
    "test:api-key": "E2E_STACK_MODE=api-key playwright test",
    "test:oidc": "E2E_STACK_MODE=oidc playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
