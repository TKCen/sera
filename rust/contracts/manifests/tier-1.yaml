apiVersion: sera/v1
kind: SandboxBoundary
metadata:
  name: tier-1
spec:
  linux:
    capabilities: ["NET_RAW", "SYS_CHROOT"]
    readOnlyRootfs: true
  allowedImages:
    - "*"
  capabilities:
    network-allowlist: ["*"]
    command-denylist: ["rm -rf /", "mkfs*"]
